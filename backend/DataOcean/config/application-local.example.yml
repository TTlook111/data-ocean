spring:
  datasource:
    url: jdbc:mysql://localhost:3307/dataocean?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: change-me
  data:
    redis:
      host: localhost
      port: 6379
      password:

jwt:
  secret: change-me-to-a-local-secret-at-least-32-bytes
  expiration: 86400
