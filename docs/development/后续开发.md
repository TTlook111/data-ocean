# 后续开发计划

> 更新日期：2026-09-12
>
> **本文档只记录未完成的事项。** 已完成或已取消的任务从正文彻底删除，不保留完成记录——那些看 git history、`CLAUDE.md`、`项目真实状态看板.md` 和 `docs/review/` 下的审查报告。
>
> P 系列按执行优先级组织，不追求编号连续；编号有跳跃是因为历史任务已完成并删除，不代表缺少任务定义。

---

## 当前未完成的工作

### 轨道 A：环境与验收（当前主线）

**阶段 0–8 的代码已于 2026-09-12 全部实现**，逐阶段证据见 `docs/development/stage-summaries/`。剩余的是**环境与验收**，不是开发：

1. **恢复可运行环境**（阻塞项）—— 补 `docker-compose.yml`（MySQL 8 + Redis + Milvus Standalone + etcd + MinIO）并修正 README / CLAUDE.md 的启动说明。在此之前所有需要 MySQL / Redis / Milvus 的运行时验证都做不了。

2. **验收阶段 0–8 的全部交付** —— 完整清单见 `项目真实状态看板.md`「第 1 步」，按「后端与迁移 → 接口级 → 前端点验 → 补截图」四段组织。要点：
   - `V51` 与 `V52`（`V52__add_query_suggested_questions.sql`）迁移从未在真实 MySQL 上执行过（首次迁移前建议备份 `knowledge_doc_version`）
   - P0 回滚校验：对 `DRAFT` 文档回滚应被拒绝
   - 7 个新增/变更接口的接口级验证（`review-tasks`、`vector-tasks`、`source-snapshots`、`prompt-templates/{code}/enabled`、`glossary/terms/{id}/revert`、`quality-issues` 的 `assigneeId`、`access-approvals` 的范围收窄）
   - 「猜你想问」推荐问题链路（`suggestedQuestions`）：Python 产出 → `query_task.suggested_questions` 落库 → `QueryTaskVO` 返回 → 前端渲染，以及历史消息刷新后的恢复
   - 阶段 5–8 的规范一致性点验（Tab 数量与名称、URL 状态恢复、双数据源选择器已消除、告警规则区无伪造统计、连接池重置二次确认）
   - **补齐阶段 0–8 的关键流程截图**（`output/playwright/`，全部缺这一项交付物）

3. **把验收结果写回各阶段总结** —— 回填 `docs/development/stage-summaries/` 各文档的「验证结果」一节，并更新状态看板。**验收完成前不得把任何阶段写成已通过。**

**仍属开发性质、但已被阶段 8 明确排除的收尾项**（可在验收后按需处理）：资产目录与术语关联字段仍用客户端过滤，可切到 `/search?datasourceId=`（后端已支持，见 `开发指导` §12.1）。

### 轨道 B：权限体系重构（轨道 A 完成后）

先更新《DataOcean 完整权限体系设计》再实施，不要从零写。

前后端必须成对改：前端当前按 `开发指导` §13 硬编码「真实 `*` 超级管理员」假设，且 `guards.ts` 中故意不实现 `meta.permission`，新模型落地时前端需专门做一次「消费权限」改造——在 7 个业务域、各二级工作区、各 Tab 和写操作上接入权限点。

前置原因：新权限模型要定义菜单权限、页面权限、Tab 权限、操作权限、数据范围五类，其划分依据就是业务域 / 工作区 / Tab 的最终形态。结构还在动的时候设计权限粒度会返工。

**归属本轨道一并处理**：`开发指导` §3.11 的最终权限预览决策端点、§3.13 的权限项。

### 轨道 C：功能待办（轨道 A 之后按需）

按顺序：

1. **P9** 告警触发执行逻辑 → **P12** 告警规则与历史前端页面（P12 依赖 P9），任务定义见下方
2. **P3** PostgreSQL 数据源支持，任务定义见下方
3. **Agent workflow 测试补强** —— 覆盖 query rewrite、SQL 生成/校验/执行、图表 fallback、RAG 降级和 Java 查询集成
4. **RAG 真实 Milvus 验证与效果评测** —— 需在现有 Milvus 上完成旧 Collection 重建/索引验证，并建立 golden questions、Hit@K/MRR/nDCG 基线
5. **RAG 全量重建入口** —— 原 `/re-vectorize` 是只回显请求的占位实现，已于 2026-09-12 连同前端入口一并移除（审查报告 A5）。若日后重开，需要先实现真实编排：读取 chunks → 分批调用 `/vectorize` → 汇报进度与失败原因，且必须支持 `doc_id`/版本级重建，不得写成「先删旧向量再写新向量」
6. **P4** 图数据库血缘迁移（按需，任务定义见下方）

### 预估总工期

| 轨道 | 预估 | 说明 |
|-------|------|------|
| A1 恢复环境 | 0.5-1 天 | 补 compose + 修正文档 |
| A2 阶段 0–8 验收 | 3-5 天 | 后端与迁移、接口级、前端点验、补截图四段；**验收是大头** |
| B 权限体系重构 | 待评估 | 需先更新设计文档后再估 |
| C 功能待办 | 9-14 天 | P9 / P12 / P3 / 测试 / RAG 验证 |

### 任务依赖关系

```
A1 (恢复 compose) ─────────────→ 阻塞 A2 与一切运行时验证
  │
  └─→ A2 (验收阶段 0–8 的全部交付) ─→ 冻结信息架构（结论写入各阶段总结）
        │
        └─→ B (权限体系重构) ───────→ 硬前置：信息架构冻结
                                       前端需同步做「消费权限」改造

C (P9 → P12 → P3 → 测试 / RAG 验证 → P4) → A 之后按需；P12 依赖 P9
```

---

## 待产品确认的取舍

**问题中心的「分配给我的」是显式筛选，不是默认开启。**

`开发指导` §7.7 原文是「默认**优先**展示高危、阻断和分配给当前用户的问题」。当前实现为筛选按钮（默认关）：强制默认过滤会让多数管理员首次进入就看到空列表，反而不知道系统里存在问题；而规范的意图是「优先」而非「只显示」，要真正落实需后端按责任人排序。

- 若要求「默认开启」：改 `frontend/src/views/admin/governance/IssueList.vue` 的 `toggleMyIssues()`，在 `onMounted` 里默认选中。
- 若要求「按责任人排序」：需后端 `QualityIssueServiceImpl.listIssues` 的 `orderBy` 增加排序参数。

---

## P3 — PostgreSQL 数据源支持

> **远期规划**：项目处于 MVP 阶段，MySQL 是唯一数据源。PostgreSQL 支持涉及元数据采集器、SQL 方言、连接池的全面适配，应在核心功能稳定后再考虑。

**改动范围**：

- 数据源模块增加 PostgreSQL 类型支持
- 元数据采集器适配 PostgreSQL 的 `information_schema` 和 `pg_catalog`
- SQL 沙箱适配 PostgreSQL 方言（sqlglot 已支持）
- 连接池管理适配 PostgreSQL JDBC 驱动

**文档更新**：完成后更新 CLAUDE.md 的 MVP scope 描述

---

## P4 — 数据血缘图数据库

> **远期 / 按需**：基于 SQL 的数据血缘已实现。P4 特指当 SQL JOIN 查询性能成为瓶颈时，把存储和查询引擎从 MySQL 迁移到图数据库。当前血缘数据量不大，SQL JOIN 可满足需求；引入图数据库需要新的运维成本。

**触发条件**：血缘关系超过数万条、SQL JOIN 查询响应 > 2s。

**改动范围**：

- 引入图数据库（如 Neo4j 或 NebulaGraph）
- 将表/列血缘关系写入图数据库
- 血缘查询从 SQL JOIN 改为图遍历
- 前端血缘可视化适配图数据库查询结果

---

## P9 — 告警触发执行逻辑

> P12 依赖本任务的执行与历史数据。

**背景**：后端 `alert_rule` 表和 `AlertController` 已实现 CRUD + toggle，但缺少定时扫描 metric 并触发通知的执行逻辑。告警规则定义了 ERROR_RATE / SLOW_QUERY_COUNT 两种指标和阈值（实体字段名为 `metric`），但没有后台任务去比对和触发。

> 注：Controller 实际类名为 `AlertController`（非 `AlertRuleController`），API 路径为 `/api/admin/alert-rules`。

**改动范围**：

1. 新增 `AlertEvaluateJob` 定时任务（每 5 分钟执行）：
   - 读取所有启用的 `AlertRule`
   - 按 `metric` 字段查询对应指标（ERROR_RATE 查 query_audit_log 的成功率、SLOW_QUERY_COUNT 查慢查询数）
   - 与阈值比较，触发时调用 `NotificationService.send()` 发通知
   - 记录告警历史（避免重复告警：同一规则在恢复前只告警一次）

2. 新增 `alert_history` 表：Flyway 脚本 `V53__create_alert_history.sql`（**注意：V52 已被 `V52__add_query_suggested_questions.sql` 占用**，本计划原写的 V52 编号作废），字段 rule_id、triggered_at、resolved_at、metric_value、threshold_value

**涉及文件**：
- 新增 `backend/.../audit/service/impl/AlertEvaluateJob.java`
- 新增 `backend/.../audit/entity/AlertHistory.java`
- 新增 `backend/.../audit/mapper/AlertHistoryMapper.java`
- 新增 `src/main/resources/db/migration/V53__create_alert_history.sql`

**文档更新**：完成后更新 CLAUDE.md 的 audit 模块描述

---

## P12 — 告警规则与历史前端管理页面

> 建议在 P9 后实施，避免前端展示已经能配置但后端尚未真正执行的规则。

**背景**：后端 `AlertController` 已实现 REST API；阶段 7 已把**告警规则的列表、创建、编辑、启停**接入运行监控工作区（`/admin/platform/runtime`）。本任务**剩余的是告警历史列表**——它依赖 P9 的告警执行与历史数据，P9 未做。

> 注：告警规则 CRUD API 函数已在 `frontend/src/api/admin/audit.ts` 中（`listAlertRules`、`createAlertRule`、`updateAlertRule`、`toggleAlertRule`），可直接复用，不需新建 `alert.ts`。

**改动范围**：

1. 页面：**告警历史列表**（规则列表、创建/编辑、启用/禁用 toggle 已于阶段 7 落地）

   > ⚠️ **勘误（2026-09-12）**：本条原写「创建/编辑/**删除**」，但后端 `AlertController` **只有列表 / 创建 / 更新 / 启停四个端点，没有删除**。阶段 7 据此**不提供删除按钮**，并在页面说明该缺口。若将来补上删除端点，前端需同步加回操作。
2. 路由和导航：在 `router/index.ts` 添加路由；不新增侧边栏一级入口，按 `completed/后台信息架构与导航规范.md` 判断归属后加入二级工作区导航（告警历史归「运营与安全」，告警规则如作为平台配置项归「系统设置」）

**涉及文件**：
- 复用 `frontend/src/api/admin/audit.ts`
- 新增告警规则与历史页面组件
- 修改 `frontend/src/router/index.ts`

**文档更新**：完成后更新 AGENTS.md 的路由树和前端 API 模块列表

---

## 质量扫描未修复项（2026-08-05 扫描，2026-09-12 复核）

> 异常处理、安全、性能、配置四维度扫描的结果。下表只列仍未修复的项。

### 🔴 严重

| 文件 | 行号 | 问题 |
|---|---|---|
| `SnapshotLifecycleServiceImpl.java` | L182 | `catch (Exception ignored) {}` — 查询审核人姓名失败静默跳过 |
| `QueryController.java` | L150 | `submitFeedback` 仍用原始 `Map<String, String>` 接收 body（已加手工校验，但未用 DTO + `@Valid`） |

### 🟡 中等

| 文件 | 问题 |
|---|---|
| `DataQualityChecker.java` | SQL 异常用 `log.debug()` 打日志（现有 5 处），生产环境不可见 |
| `pool_manager.py` L62 | 全局连接数用 `pool_size` 配置值而非实际活跃连接数，高估实际使用量 |
| `pool_manager.py` | 多 worker 进程下全局连接数 = `pool_max_global × worker_count`（非预期行为） |
| `core/config.py` L24 | DashScope base URL 硬编码为默认值 |
| `CaptchaServiceImpl.java` | 验证码尺寸/长度/字符集、限流窗口硬编码为 `private static final`（6 处） |
| `ColumnCollector.java` L76-77 | 列名来自数据库元数据，已做反引号转义，但建议改参数化查询 |
| `PromptInternalController.java` L45-49 | `UNSAFE_DEFAULT_TOKEN` 硬编码默认值存在源码中 |

---

## 待清理项

| 项目 | 说明 |
|---|---|
| `AddLineageDialog.vue` 小 TODO | 第 209 行：预填实体 ID 时未加载实体信息 |
| `QualityScoreAggregationService` 小 TODO | 第 66 行：需注入 `MetadataSnapshotMapper` 改为 JOIN 查询过滤 |
