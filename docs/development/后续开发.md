# 后续开发计划

> 更新日期：2026-09-25
>
> **本文档只记录未完成的事项。** 已完成或已取消的任务从正文彻底删除，不保留完成记录——历史过程通过 Git history 和 `output/playwright/` 证据追溯。
>
> P 系列按执行优先级组织，不追求编号连续；编号有跳跃是因为历史任务已完成并删除，不代表缺少任务定义。

---

## 当前未完成的工作

### 轨道 B：B6 已完成，仅剩会话机制一项遗留

B6 已在 `codex/iam-s1-b6-cleanup` 分支**执行完成**（批次 0～4，2026-09-25）。执行记录、逐项依据与过程中发现的问题见 `docs/development/轨道B-B6删除清单与执行顺序.md`。

结果：旧权限体系的 Controller、Service、实体、Mapper、前端页面与 API 模块、旧问数链路（`QueryController`、`PythonAgentClientImpl`、`PermissionCalculator`、`DatasourceAccessService`）、旧角色权限配置，以及 Python 旧 Agent 包全部删除；6 张旧表由 `V58` 删除。

授权要求仍然记录在此以免被误删：**B6 必须单独获得授权**（本次授权由项目负责人在 2026-09-25 给出）。

**唯一剩余的遗留项**：冻结清单第 497/498 行的替代物 `authProtocolVersion` / `sessionEpoch` / `iam-s1:session:*` 在 Java 主代码中仍为 **0 处引用**，`jwt:blacklist:{jti}` / `user:token-version:{userId}` 仍是唯一在用的会话失效机制。这两行只完成了「删旧 claim」的一半，`tokenVersion` 仍在使用。

下一步执行顺序（本文件只列队列，不执行操作）：

1. **若要删除 `tokenVersion` 与旧会话键，必须先建立并验收替代会话机制**；反过来，不要在没有替代物时先删。设计替代机制是一项独立任务。
2. 只删除冻结清单中的旧权限专用入口、代码和表，不删除账号、部门、数据源、元数据、知识、会话、审计等业务数据。
3. 其他产品开发从更新后的 `main` 新建分支，不把本机环境验收混入其他功能分支。
4. 本机开发环境下 B5 的独立 MySQL 恢复演练与双用户负向场景已无必要（数据可弃、无生产环境）；但**其他环境**的正式切换仍须按 B5 手册逐项执行。

B5 手册和通用安全流程继续保留，仍适用于其他环境的正式切换；本队列不重新执行 Flyway、bootstrap、数据库备份或服务操作。P9 若实施必须使用 V58 或更高的未占用版本——**V58 已被 B6 占用**，因此实际应从 **V59** 起选版本；V53 永久不用。

---

## P9 — 告警触发执行逻辑

> P12 依赖本任务的执行与历史数据。
>
> **V53 决策已冻结（2026-09-21）**：V53 永久不再使用；不为填编号创建空 migration；V54～V57 执行后禁止再新增 V53。本任务若实施，使用**当时最高的未占用版本**创建 `alert_history`（V58 已被 B6 占用，故当前为 **V59** 或更高）。

**背景**：后端 `alert_rule` 表和 `AlertController` 已实现 CRUD + toggle，但缺少定时扫描 metric 并触发通知的执行逻辑。告警规则定义了 ERROR_RATE / SLOW_QUERY_COUNT 两种指标和阈值（实体字段名为 `metric`），但没有后台任务去比对和触发。

> 注：Controller 实际类名为 `AlertController`（非 `AlertRuleController`），API 路径为 `/api/admin/alert-rules`。

**改动范围**：

1. 新增 `AlertEvaluateJob` 定时任务（每 5 分钟执行）：
   - 读取所有启用的 `AlertRule`
   - 按 `metric` 字段查询对应指标（ERROR_RATE 查 query_audit_log 的成功率、SLOW_QUERY_COUNT 查慢查询数）
   - 与阈值比较，触发时调用 `NotificationService.send()` 发通知
   - 记录告警历史（避免重复告警：同一规则在恢复前只告警一次）
2. 新增 `alert_history` 表：Flyway 脚本使用**最高未占用编号**（V58 已被 B6 占用，故当前为 **V59** 或更高，例如 `V59__create_alert_history.sql`），字段 rule_id、triggered_at、resolved_at、metric_value、threshold_value。禁止使用 V53。

**涉及文件**：
- 新增 `backend/.../audit/service/impl/AlertEvaluateJob.java`
- 新增 `backend/.../audit/entity/AlertHistory.java`
- 新增 `backend/.../audit/mapper/AlertHistoryMapper.java`
- 新增 `src/main/resources/db/migration/V59__create_alert_history.sql`（编号以实施时未占用的最高版本 + 1 为准，且必须大于已应用的最大版本；本机当前最大版本为 V58）

**文档更新**：完成后更新 CLAUDE.md 的 audit 模块描述

---

## P12 — 告警规则与历史前端管理页面

> 建议在 P9 后实施，避免前端展示已经能配置但后端尚未真正执行的规则。

**背景**：后端 `AlertController` 已实现 REST API；阶段 7 已把**告警规则的列表、创建、编辑、启停**接入运行监控工作区（`/admin/platform/runtime`）。本任务**剩余的是告警历史列表**——它依赖 P9 的告警执行与历史数据，P9 未做。

> 注：告警规则 CRUD API 函数已在 `frontend/src/api/admin/audit.ts` 中（`listAlertRules`、`createAlertRule`、`updateAlertRule`、`toggleAlertRule`），可直接复用，不需新建 `alert.ts`。

**改动范围**：

1. 页面：**告警历史列表**（规则列表、创建/编辑、启用/禁用 toggle 已于阶段 7 落地）

   > 勘误（2026-09-12）：本条原写「创建/编辑/**删除**」，但后端 `AlertController` **只有列表 / 创建 / 更新 / 启停四个端点，没有删除**。阶段 7 据此**不提供删除按钮**，并在页面说明该缺口。若将来补上删除端点，前端需同步加回操作。
2. 路由和导航：在 `router/index.ts` 添加路由；不新增侧边栏一级入口。告警历史归运营与平台工作区。

**涉及文件**：
- 复用 `frontend/src/api/admin/audit.ts`
- 新增告警规则与历史页面组件
- 修改 `frontend/src/router/index.ts`

**文档更新**：完成后更新 AGENTS.md 的路由树和前端 API 模块列表
