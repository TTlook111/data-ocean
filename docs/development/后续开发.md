# 后续开发计划

> 更新日期：2026-09-25
>
> **本文档只记录未完成的事项。** 已完成或已取消的任务从正文彻底删除，不保留完成记录——历史过程通过 Git history 和 `output/playwright/` 证据追溯。
>
> P 系列按执行优先级组织，不追求编号连续；编号有跳跃是因为历史任务已完成并删除，不代表缺少任务定义。

---

## 当前未完成的工作

### 轨道 B：IAM-S1 迁移收尾与 B6 收口

B0～B4（含 B4-A 与批次 1～6）的代码与自动化验证已合入 `1f0a5f4`（含 IAM 实现提交 `1a6e426`）。当前代码切换提交为 `1e2f458`，正式旧组织导航和 `/admin/access/organization` 路由已移除；已迁移 Controller **23** 个，批次 6 实际 **61** 个 Handler，B4 自动化基线为 Java **557**、前端 Vitest **67**。

`IT-GO-1225` 本机开发环境已完成 V51 → V52 → V54 → V55 → V56 → V57 至 V57 的切换、bootstrap 和核心浏览器验收；V53 永久不使用，业务数据授权仍为 0。该结果只代表本机开发环境直接切换测试，不代表生产发布。B5 仍有独立 MySQL 恢复演练和双用户负向场景缺口。

**B6 清理已在 `codex/iam-s1-b6-cleanup` 分支开始（`fa0ca89`，已推送）但未收口。** 已完成：移除 `LoginUser`/`UserContext` 旧角色权限字段、JWT 的 `roles`/`permissions` claim、`LoginVO`/`CurrentUserVO` 角色权限字段、`UserDetailsServiceImpl` 旧权限加载、前端 `guards.ts`/`stores/auth.ts` 旧权限数组。仍保留：`DatasourcePermissionController` / `AccessPolicyController` / `AccessApprovalController` 及其服务与 Mapper、前端 `api/admin/permission.ts`、`PermissionCalculatorImpl` 的 Caffeine 权限缓存、旧权限表与其数据。

**分支定性（2026-09-25 项目负责人决定）**：`5f13efd` 不是删除提交，而是延续 IAM-S1 迁移（数据源就绪检查与查询对话，把旧权限访问逻辑替换为 S1 路径）。因此 `codex/iam-s1-b6-cleanup` 按 **IAM-S1 迁移收尾分支**理解，不拆分为纯 B6 删除分支。

一个必须先处理的问题：

- 冻结清单第 497/498 行的替代物 `authProtocolVersion` / `sessionEpoch` / `iam-s1:session:*` 在 Java 主代码中为 **0 处引用**，`jwt:blacklist:{jti}` / `user:token-version:{userId}` 仍是唯一在用的会话失效机制。这两行只完成了“删旧 claim”的一半，`tokenVersion` 仍在使用；在替代会话机制建立前不要删除它。

2026-09-25 对 `5f13efd` 的验证结果：Java `mvn test` **581** 通过、0 失败、0 错误、0 跳过；前端 Vitest **12 文件 70** 通过；`npm run build` exit code 0。

下一步执行顺序（本文件只列队列，不执行操作）：

1. **B6 必须单独获得授权**，并在授权后才能执行删除；未获授权时只做代码侧收尾。授权后决定收口范围：是继续按冻结清单完成剩余删除，还是先补齐 B5 的独立 MySQL 恢复演练和双用户负向场景。
2. 若要删除 `tokenVersion` 与旧会话键，必须先建立并验收替代会话机制；反过来，不要在没有替代物时先删。
3. 只删除冻结清单中的旧权限专用入口、代码和表，不删除账号、部门、数据源、元数据、知识、会话、审计等业务数据。
4. 其他产品开发从更新后的 `main` 新建分支，不把本机 B5 验收混入其他功能分支。

B5 手册和通用安全流程继续保留，仍适用于其他环境的正式切换；本队列不重新执行 Flyway、bootstrap、数据库备份、服务操作或 B6。P9 若实施必须使用 V58 或更高的未占用版本，V53 永久不用。

---

## P9 — 告警触发执行逻辑

> P12 依赖本任务的执行与历史数据。
>
> **V53 决策已冻结（2026-09-21）**：V53 永久不再使用；不为填编号创建空 migration；V54～V57 执行后禁止再新增 V53。本任务若实施，使用 **V58 或当时更高的未占用版本** 创建 `alert_history`。

**背景**：后端 `alert_rule` 表和 `AlertController` 已实现 CRUD + toggle，但缺少定时扫描 metric 并触发通知的执行逻辑。告警规则定义了 ERROR_RATE / SLOW_QUERY_COUNT 两种指标和阈值（实体字段名为 `metric`），但没有后台任务去比对和触发。

> 注：Controller 实际类名为 `AlertController`（非 `AlertRuleController`），API 路径为 `/api/admin/alert-rules`。

**改动范围**：

1. 新增 `AlertEvaluateJob` 定时任务（每 5 分钟执行）：
   - 读取所有启用的 `AlertRule`
   - 按 `metric` 字段查询对应指标（ERROR_RATE 查 query_audit_log 的成功率、SLOW_QUERY_COUNT 查慢查询数）
   - 与阈值比较，触发时调用 `NotificationService.send()` 发通知
   - 记录告警历史（避免重复告警：同一规则在恢复前只告警一次）
2. 新增 `alert_history` 表：Flyway 脚本使用 **V58 或更高未占用编号**（例如 `V58__create_alert_history.sql`），字段 rule_id、triggered_at、resolved_at、metric_value、threshold_value。禁止使用 V53。

**涉及文件**：
- 新增 `backend/.../audit/service/impl/AlertEvaluateJob.java`
- 新增 `backend/.../audit/entity/AlertHistory.java`
- 新增 `backend/.../audit/mapper/AlertHistoryMapper.java`
- 新增 `src/main/resources/db/migration/V58__create_alert_history.sql`（编号以实施时未占用的最高版本 + 1 为准，且必须大于已应用的最大版本）

**文档更新**：完成后更新 CLAUDE.md 的 audit 模块描述

---

## P12 — 告警规则与历史前端管理页面

> 建议在 P9 后实施，避免前端展示已经能配置但后端尚未真正执行的规则。

**背景**：后端 `AlertController` 已实现 REST API；阶段 7 已把**告警规则的列表、创建、编辑、启停**接入运行监控工作区（`/admin/platform/runtime`）。本任务**剩余的是告警历史列表**——它依赖 P9 的告警执行与历史数据，P9 未做。

> 注：告警规则 CRUD API 函数已在 `frontend/src/api/admin/audit.ts` 中（`listAlertRules`、`createAlertRule`、`updateAlertRule`、`toggleAlertRule`），可直接复用，不需新建 `alert.ts`。

**改动范围**：

1. 页面：**告警历史列表**（规则列表、创建/编辑、启用/禁用 toggle 已于阶段 7 落地）

   > 勘误（2026-09-12）：本条原写「创建/编辑/**删除**」，但后端 `AlertController` **只有列表 / 创建 / 更新 / 启停四个端点，没有删除**。阶段 7 据此**不提供删除按钮**，并在页面说明该缺口。若将来补上删除端点，前端需同步加回操作。
2. 路由和导航：在 `router/index.ts` 添加路由；不新增侧边栏一级入口。告警历史归运营与平台工作区。

**涉及文件**：
- 复用 `frontend/src/api/admin/audit.ts`
- 新增告警规则与历史页面组件
- 修改 `frontend/src/router/index.ts`

**文档更新**：完成后更新 AGENTS.md 的路由树和前端 API 模块列表
