# DataOcean 后台前端重构：原能力迁移对照表

> 编制日期：2026-09-12
> 用途：《实施任务清单》§15 强制交付物；对应 §16 最终完成条件第 5 条「旧页面能力迁移无遗漏，旧 URL 已兼容」
> 编制方式：以**已删除的旧文件**与**阶段 1–7 的实施记录**为准逐条对照，而非以旧菜单或旧 URL 为准

## 一、旧视图 → 新归属（15 个已删除文件）

这 15 个文件在阶段 8 被删除，前置条件是能力已进入新页面。删除前做过传递可达性分析确认零引用。

| 旧视图 | 阶段 | 新归属 | 能力承载 |
| --- | --- | --- | --- |
| `datasource/DatasourceList.vue`（937 行） | 2 | `DataSourcesView.vue` | 数据源列表、创建、编辑、启停、健康状态 |
| `datasource/DatasourceLifecycle.vue`（931 行） | 2 | `DataSourceDetailView.vue` | 上线驾驶舱、就绪度、生命周期步骤、下一步引导 |
| `metadata/CatalogSearch.vue`（237 行） | 3 | `assets/AssetsView.vue` | 资产目录搜索与浏览 |
| `metadata/SnapshotList.vue`（120 行） | 3 | `releases/ReleasesView.vue` 候选快照 Tab | 快照列表与状态 |
| `metadata/SnapshotLifecycle.vue`（315 行） | 3 | `releases/ReleasesView.vue` | 快照检查、审核、发布、撤回 |
| `metadata/VersionHistory.vue`（213 行） | 3 | `releases/ReleasesView.vue` 版本历史 Tab | 版本历史与审计 |
| `metadata/SnapshotDiff.vue`（144 行） | 3 | `releases/SnapshotDiffView.vue` | 快照差异比较 |
| `glossary/GlossaryList.vue`（424 行） | 5 | `semantics/GlossariesView.vue` | 术语表/术语 CRUD、审核、字段关联（旧实现是单表格，新实现按「术语表 → 术语 → 关联字段 → 审核状态」分层） |
| `knowledge/KnowledgeDashboard.vue`（404 行） | 5 | `semantics/KnowledgeView.vue` | 知识文档列表、AI 生成、语义准备情况总览 |
| `knowledge/ReviewPage.vue`（183 行） | 5 | `semantics/KnowledgeView.vue` 审核队列 Tab | 审核队列（从孤立页面变为工作区 Tab） |
| `knowledge/SkillsEditor.vue`（302 行） | 5 | `semantics/KnowledgeDocView.vue` | 文档编辑、AI 草稿、提交审核、发布 |
| `knowledge/VersionList.vue`（162 行） | 5 | `semantics/KnowledgeDocView.vue` 版本 Tab | 版本列表、差异、回滚 |
| `prompt/PromptManager.vue`（965 行） | 5 | `semantics/PromptsView.vue` | Prompt 模板列表/详情/审核/版本/回滚/启停 |
| `audit/LineageGraph.vue`（298 行） | 7 | `audit/DataLineage.vue` | 血缘可视化图谱 |
| `audit/LineageViewer.vue`（200 行） | 7 | `audit/DataLineage.vue` | 血缘搜索与**影响分析**（本轮补回入口） |

**结论**：15 个旧视图的能力全部有新归属，无遗漏。

## 二、旧组件与基础设施 → 新归属

| 旧组件 | 新归属 | 说明 |
| --- | --- | --- |
| `components/AppShell.vue`（771 行） | `components/admin/AdminShell.vue` | 旧壳层，全仓零引用 |
| `components/AdminContextBar.vue`（183 行） | `components/admin/ScopeBar.vue` | 范围选择与锁定说明 |
| `components/AdminBreadcrumb.vue`（84 行） | `AdminWorkspaceNav` + `ObjectContextSummary` | **位置感知能力未丢失，换了承载形式**：二级工作区导航表达「我在哪个工作区」，`ObjectContextSummary` 表达「我在哪个对象上、怎么返回」 |
| `components/common/PageHeader.vue`（64 行） | `components/admin/TaskPageHeader.vue` | 统一页面标题区（§10.1） |
| `composables/useApiRequest.ts` / `usePagination.ts` / `useQuerySse.ts` | 无（删除） | 零引用；各页面用自己的 `requestId` 防竞态模式与分页状态 |
| `stores/crud.ts` | 无（删除） | 零引用 |
| `utils/error.ts` | `api/admin/*` 各模块内的 `apiError`/`extractError` | 零引用；错误提取在各页面已就地实现 |
| `styles/breakpoints.css` + `assets/breakpoints.css` | `style.css` 内的媒体查询 | 两份同名不同内容的死文件，且断点集互不一致 |

## 三、路由迁移与重定向（§9 全部条目）

**29 条旧 URL 全部有重定向**，且保留对象 ID、Tab 与查询参数。阶段 8 复核时逐条确认。

| 旧 URL | 目标 |
| --- | --- |
| `/admin` | `/admin/workbench` |
| `/admin/datasources` | `/admin/data-sources` |
| `/admin/datasources/:id/lifecycle` | `/admin/data-sources/:id` |
| `/admin/metadata/sync` | `/admin/collections` |
| `/admin/metadata/schedule` | `/admin/collections?tab=schedule` |
| `/admin/metadata/catalog` | `/admin/assets` |
| `/admin/metadata/tables` | `/admin/assets` |
| `/admin/metadata/lifecycle` | `/admin/releases` |
| `/admin/metadata/snapshots` | `/admin/releases` |
| `/admin/metadata/version-history` | `/admin/releases?tab=history` |
| `/admin/metadata/diff` | 新快照差异路由（保留原查询参数转换） |
| `/admin/governance/quality` | `/admin/governance` |
| `/admin/governance/issues` | `/admin/governance/issues`（保持不变） |
| `/admin/governance/status` | `/admin/governance/rules?tab=status` |
| `/admin/field/tags` | `/admin/governance/fields?tab=tags` |
| `/admin/field/confidence` | `/admin/governance/fields?tab=confidence` |
| `/admin/field/feedback-review` | `/admin/governance/fields?tab=feedback` |
| `/admin/glossary/list` | `/admin/semantics/glossaries` |
| `/admin/knowledge` | `/admin/semantics/knowledge` |
| `/admin/knowledge/editor/:id` | `/admin/semantics/knowledge/:id?tab=content` |
| `/admin/knowledge/versions/:id` | `/admin/semantics/knowledge/:id?tab=versions` |
| `/admin/knowledge/review` | `/admin/semantics/knowledge?tab=review` |
| `/admin/prompts` | `/admin/semantics/prompts` |
| `/admin/permission/access` | `/admin/access?tab=grants` |
| `/admin/permission/policies` | `/admin/access?tab=policies`（**本轮由独立页面改为重定向**） |
| `/admin/users` | `/admin/access/organization?tab=users` |
| `/admin/roles` | `/admin/access/organization?tab=roles` |
| `/admin/departments` | `/admin/access/organization?tab=departments` |
| `/admin/audit/logs` | `/admin/operations/queries?tab=audit` |
| `/admin/audit/slow-queries` | `/admin/operations/queries?tab=performance` |
| `/admin/audit/data-lineage` | `/admin/operations/lineage` |
| `/admin/system/health` | `/admin/platform/runtime` |
| `/admin/system/operation-logs` | `/admin/platform/operation-logs` |
| `/admin/system/ai-config` | `/admin/platform/ai` |

## 四、跨域重组：旧页面之间合并的能力

以下能力在旧结构里分散在多个页面，新结构按业务域重新组织：

| 能力 | 旧位置 | 新位置 |
| --- | --- | --- |
| 同步任务 + 同步调度 | `metadata/SyncTask.vue` 与独立调度页 | `collections/CollectionsView.vue` 的任务列表 + 调度 Tab |
| 质量规则启停 + 表字段治理状态 | `governance/QualityDashboard` 与 `metadata/StatusEditor` | `governance/StatusEditor.vue` 的「质量规则 / 资产状态」两个 Tab |
| 标签 + 可信度 + 反馈审核 | 三个独立 `field/*` 页面 | `governance/GovernanceFieldsView.vue` 的四个 Tab |
| 数据源授权 + 表列策略 + 最终权限预览 | `permission/AccessControl.vue` + `permission/PolicyEditor.vue`（孤儿路由） | `permission/AccessControl.vue` 的三个固定 Tab（**阶段 6 合并**） |
| 服务健康 + SQL 连接池 + 告警规则 | `system/ServiceHealth.vue`（扁平单页） | `system/RuntimeView.vue` 的三个固定 Tab（**阶段 7 重建**） |
| 查询审计 + 性能分析 | `audit/AuditLogList` + `audit/SlowQueryList` | `audit/QueryAnalysisView.vue` 的两个 Tab |
| 操作日志 | `system/OperationLogList.vue` | 保留为独立工作区（与查询审计语义不同，§7.19 明确要求保留） |

## 五、能力缺口：无法承载的项（如实记录，未伪造）

以下能力**在前后端两侧都不成立**，因此没有前端归属，也不会出现在页面上：

| 缺口 | 后端事实 | 前端处理 |
| --- | --- | --- |
| 分类体系 CRUD | 未发现独立的分类体系 Controller | 不制作虚假的分类管理页（§12）；字段标签继续使用现有接口 |
| 部门成员摘要 | `DepartmentController` 只有部门树与增删改，无成员接口 | 「部门」Tab 内说明缺失，并引导到「用户」Tab 按部门筛选 |
| 告警执行历史与恢复闭环 | 无告警执行记录、历史、恢复、去重接口 | 运行监控的告警规则 Tab 显式标注「只支持规则配置」，不显示任何执行统计 |
| 向量化任务进度百分比 | `vector_index_task` 无进度列 | 索引状态展示状态与起止时间，并说明无法给出百分比 |
| 术语审核意见 | `glossary_term` 无存放拒绝原因的列 | 审核记录展示状态/审核人/时间，并说明审核意见不可得 |
| 授权与策略的优先级、策略有效期 | `DatasourcePermissionVO` 无 `priority`；`AccessPolicyVO` 未暴露 `priority`/`validFrom`/`validUntil` | 授权管理页如实标注这两项无数据来源 |
| 审批的治理状态复查结果 | 后端审批时执行复查，但不回写申请记录 | 审批详情说明复查存在但结果不可得，不伪造「复查通过」标记 |
| 血缘边的编辑 | 后端只有创建与删除血缘的接口，无更新接口 | 移除右键菜单的「编辑」项，而不是保留一个必然失败的动作 |
| 告警规则的删除 | `AlertController` 只有列表/创建/更新/启停，无删除端点 | 不提供删除按钮，页面说明缺口 |
| 普通用户「我的申请」 | 列表范围已由后端收窄，但入口开放需产品确认（§7.14） | API 封装已就绪，暂不开放入口 |

## 六、核验结论

| 核验项 | 结论 |
| --- | --- |
| 旧视图能力是否有遗漏 | **无遗漏**（15 个全部有新归属） |
| 旧 URL 是否兼容 | **29 条全部有重定向**，保留 ID/Tab/查询参数 |
| 旧菜单入口是否仍在 | 否。侧边栏只有七个一级业务域；旧 URL 仅作兼容重定向，不再有链接指向 |
| 是否存在超纲死代码 | 阶段 8 删除后由脚本复核：无不可达的代码文件 |
| 无法承载的能力是否如实记录 | 是，见第五节 10 项；页面内均有说明，未伪造字段 |
| 是否已有运行时验证 | **否**。本表的「能力承载」结论均基于静态阅读与构建验证 |
