# DataOcean 项目真实状态看板

> 更新日期：2026-09-12
>
> 对应代码快照：`main` / `4d710cc`（2026-09-11），另加分支 `fix/frontend-defect-list` 上的 9 项前端缺陷修复（提交 `22a1be5`）。
>
> 本文档是当前完成度、验证结果、已知风险和开发优先级的唯一状态入口。`specs/` 目录是早期设计资料，仅用于追溯，不作为当前代码范围或完成度依据。

## ⚠️ 当前最高优先级阻塞项：没有可运行环境

仓库内**不存在任何 `docker-compose.yml`**（已确认全仓检索无结果），但 `README.md` 的「快速启动」和 `CLAUDE.md` 的「Development Commands」都让人执行：

```bash
docker compose up -d
```

这带来两个后果：

1. **所有依赖 MySQL / Redis / Milvus 的运行时验证都无法执行** —— 包括后台前端重构 §16 的四组验收场景、Agent 全链路、RAG 真实 Milvus 验证。这是后续每一步的共同前置。
2. **新克隆仓库的人第一步就会卡住** —— 对「开源项目」目标（见 `后续开发.md` 的执行口径）是硬伤。

`后续开发.md` 的「待清理项」曾记录此项并判定「P8 已取消，暂不规划，本地按需自行启动服务即可」。该判定在「只需要写代码」的阶段成立，但在需要验收的阶段不再成立，**建议改为优先补齐**。注意 Milvus Standalone 还需要 etcd 与 MinIO 两个伴生服务。

## 验证基线

| 项目 | 命令 | 当前结果 |
| --- | --- | --- |
| 前端生产构建 | `cd frontend && npm run build` | ✅ **2026-09-12 通过**（`vue-tsc -b && vite build`）；仍有大包告警（ECharts 与主入口包超过 500 kB） |
| Java 单元测试 | `cd backend/DataOcean && mvn test` | ✅ **2026-09-12 复审修复后重跑：145 个测试、0 失败、0 错误、0 跳过**。注意该套件在阶段 5–8 修复轮中曾**整体编译失败**（`GlossaryTermServiceImplTest` 的 `insert(any())` 重载歧义），当时任何「已修复」声明都没有可执行证据；修好后为 145 项（32 个测试类，仅 1 个 `@SpringBootTest`，其余为 Mockito 单测）。当日更早的基线为 143，再早为 119 |
| Python 单元测试 | `cd python-service && uv run pytest -q` | ⚠️ **数字有冲突待重跑**：`CLAUDE.md` 记 152 通过 / 4 跳过，本文档原记 148 通过 / 4 跳过（均为 2026-09-07 基线） |
| Python 依赖锁 | `cd python-service && uv lock --offline --check` | 通过（2026-09-07 基线，本轮未重跑） |

**离线可用性（2026-09-12 实测）**：

- **Java 测试不需要任何外部服务** —— 测试栈以 Mockito 单测为主，另有 H2 + `src/test/resources/application-test.yml` + `schema.sql` 支撑唯一一个 `@SpringBootTest`。因此在没有 MySQL / Redis / Milvus 的情况下，后端缺陷的修复可以**先写代码并跑单测**，把运行时验证留到环境恢复后一次性做。
- **前端构建不需要任何外部服务**，但**不覆盖任何行为**：项目装了 `vitest` 而零测试用例。
- 依赖真实服务的验证全部被阻塞（见文首）。

> 前端构建通过**不构成行为验证**：项目装了 `vitest` 但**零测试用例**，`npm run build` 只做类型检查与打包，不覆盖任何运行时行为。
>
> 测试运行时禁止向 LangSmith 发送追踪数据，以保证本地 `.env` 中的开发配置不会影响测试结果。

## 当前模块状态

| 模块 | 功能状态 | 验证状态 | 主要待办 |
| --- | --- | --- | --- |
| 用户、认证与权限 | 主链路完成 | Java 单测覆盖部分核心服务 | 新权限体系设计（见下） |
| 数据源、元数据与治理 | 主链路完成 | Java 单测覆盖有限 | 数据级质量检查与治理联动的集成测试 |
| skills.md 与 RAG | 发布/向量化链路完成；审核与索引可观测性已补齐 | Python RAG 回归测试通过；Java 新增 13 个测试 | 真实 Milvus Collection 重建/集成验证、性能基线、golden questions；**V51 迁移未在真实 MySQL 执行过** |
| 知识文档审核与回滚 | 2026-09-12 修复：回滚补前置校验、审核意见可查、`review_status` 真实写入 | Java 单测覆盖（`KnowledgeDocLifecycleServiceTest` 4 项、重写后的 `KnowledgeVersionServiceImplTest` 7 项） | 运行时验证；G2（按文档查来源快照的聚合接口）未做，前端已有可用组合实现 |
| 业务术语 | 2026-09-12 修复：状态机收敛（方案 A）、退回接口、删除清理关联 | Java 单测覆盖（`GlossaryTermServiceImplTest` 9 项，此前该模块无测试） | 运行时验证 |
| NL2SQL 与 SQL 沙箱 | 主链路完成 | Python 单测通过 | Agent 全链路、取消和降级集成测试 |
| 审计与数据血缘 | 核心功能完成；2026-09-12 修复两处丢失 `datasourceId` 导致的错误列血缘 | 单测覆盖有限 | 修复 `AddLineageDialog` 预填实体的小待办 |
| 告警 | 仅规则 CRUD 完成 | 尚无执行验证 | P9：定时评估、通知去重、恢复、历史 |
| 后台前端重构 | **阶段 0–8 代码层面已实现；运行时验收整体未做** | 2026-09-12 构建通过；**无任何运行时验收记录** | 见下方「第 1 步」的验收清单 |

### 后台前端重构的实际进度

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 0 | 建立基线 | ✅ 代码已实现 |
| 1 | 壳层与工作台 | ✅ 代码已实现 |
| 2 | 数据源上线主流程 | ✅ 代码已实现 |
| 3 | 资产与版本发布 | ✅ 代码已实现 |
| 4 | 治理闭环 | ✅ 代码已实现 |
| 5 | 语义发布闭环 | ✅ 代码已实现（2026-09-12 补齐 12 项规范缺口，提交 `6f939f8`） |
| 6 | 权限与组织 | ✅ 代码已实现（2026-09-12 重建三个工作区，提交 `8e99bbb`） |
| 7 | 运营与平台 | ✅ 代码已实现（2026-09-12 重建五个工作区，提交 `c6f3eea`） |
| 8 | 迁移与收敛 | ✅ 代码已实现（2026-09-12 删除 27 个死文件、收敛重复封装与令牌，提交 `b6587f9`） |

**逐阶段的实施证据与残留缺口见 `docs/development/stage-summaries/`**：

| 文档 | 内容 |
| --- | --- |
| `阶段5-实施总结.md` | 语义中心 12 项规范缺口的补齐记录 |
| `阶段6-实施总结.md` | 权限与组织三个工作区的重建记录 |
| `阶段7-实施总结.md` | 运营与平台五个工作区的重建记录 |
| `阶段8-实施总结.md` | 死代码删除、重复封装收敛与令牌统一 |
| `原能力迁移对照表.md` | §15 强制交付物：15 个旧视图 / 8 个旧组件 / 34 个旧 URL 兼容表项（33 条重定向 + `/admin/governance/issues` 保持原路由）→ 新归属，含 10 项能力缺口 |

> ⚠️ **「代码已实现」不等于「已验证」。** 阶段 0–8 **从未做过任何浏览器验收**，`开发指导` §17 完成定义第 9 条（真实数据 + 超级管理员完整流程的浏览器验证）与 §15 的截图交付要求**均未满足**。验收清单见下方「第 1 步」。
>
> ⚠️ **方法论提醒（2026-09-12 的一次实际误判）**：本文档曾一度把「文件存在 + 路由指向它 + 行数可观」当作阶段 6–7 已落地写入，该判断被项目所有者纠正——那些是重构前的旧实现被路由指了过去。判断阶段完成度必须对照开发指导的目标设计**逐项核对**，不能以文件是否存在、行数多少、路由是否可达为依据。阶段 6–7 因此是**重建**而非补缺口。
>
> 前端缺陷清单 F1–F9 已于 2026-09-12 修复（提交 `22a1be5`），详见 `docs/review/2026-09-11-后台前端重构审查-前端缺陷清单.md` 的 §7.1。

## 当前开发顺序

顺序的判据不是「哪块更想先做」，而是**哪块阻塞其他块**。后台前端重构的信息架构是权限体系设计的硬前置（`开发指导` §17 完成定义第 10 条、§18 禁止事项），所以前端必须先冻结。

### 第 0 步：恢复可运行环境（阻塞项，不解决则后面都无法验证）

补 `docker-compose.yml`（MySQL 8 + Redis + Milvus Standalone + etcd + MinIO），并同步修正 README / CLAUDE.md 的启动说明。

### 第 1 步：验收阶段 0–8 的全部交付（环境恢复后立刻做）

**这是当前唯一的主线任务。** 阶段 0–8 的代码已全部实现，但**没有做过任何浏览器验收**。以下清单按「先核对后端事实、再点验前端、最后补截图」组织。

#### 1.1 先跑后端与迁移（阻塞其余一切）

1. **`mvn test`** 确认 143 个单测仍通过（不需要外部服务）。
2. **`V51` 与 `V52` 迁移从未在真实 MySQL 上执行过。** 首次启动时观察迁移是否成功：`V51` 检查回填结果是否符合预期（能从 `knowledge_review_task` 还原的行按其最新审核任务还原，其余为 `UNKNOWN`）；`V52` 检查 `query_task.suggested_questions` 是否按 `JSON` 类型建立在 `masked_fields` 之后。**迁移前先备份 `knowledge_doc_version` 表。**
3. **P0 回滚校验**：对 `DRAFT` 文档调用 `POST /knowledge-docs/{id}/rollback`，应被拒绝（这是原先的漏洞路径）。

#### 1.2 接口级验证（7 个新增/变更接口）

- `GET /knowledge-docs/{id}/review-tasks`、`/vector-tasks`、`/source-snapshots`
- `PATCH /prompt-templates/{code}/enabled`、`POST /glossary/terms/{id}/revert`
- `GET /quality-issues` 与 `/snapshots/{id}/quality-issues` 的 `assigneeId` 参数
- `GET /access-approvals` 的范围收窄（用非 `security:manage` 账号确认只看得到自己提交的申请）

#### 1.3 前端点验（按阶段）

| 阶段 | 重点 |
| --- | --- |
| 1–4 | 按 `开发指导` §16 四组场景（数据源上线、上下文隔离、生命周期、异常体验）走一遍；这部分从未验证过 |
| 5 | 知识详情 **6 个 Tab** 的名称与顺序；术语页与 Prompt 页的 **URL 状态恢复**（刷新 + 浏览器后退）；知识列表的「数据源知识准备情况」总览 |
| 5（缺陷） | 前端缺陷 F1–F9 的 9 项（尤其 F1 的 8 种 readiness 状态、F6 的跨工作区 URL 继承、F2 需构造 `ISSUE_FOUND` 快照） |
| 6 | 授权管理 **3 Tab** 与主体驱动流程；**§16.4「管理员不会因顶部和页面数据源不一致而误授权」**（本轮删掉了页面自建的第二个数据源选择器，需实机确认）；访问审批的详情与「通过」二次确认 |
| 7 | 运行监控 **3 Tab**；**连接池重置的二次确认**；**告警规则区不显示伪造统计**；血缘的方向控件与影响分析；AI 配置的维度检测与重新向量化 |
| 8 | 旧 URL 兼容表项（34 个：33 条重定向 + `/admin/governance/issues` 保持原路由）逐个打开确认保留 ID/Tab/参数；确认侧边栏不再有旧入口 |

**无法在浏览器构造、只能代码级确认的项**：`known === false`（F8，运行态不可达）、`stage === 'UNKNOWN'`（F7，后端不可达）、`SIGN'` 等无法构造的后端异常分支。

#### 1.4 补齐截图

按 `实施任务清单` §14 命名规则存入 `output/playwright/`（目录不存在，需先建）。**阶段 0–8 全部缺这一项交付物。**

### 第 2 步：把验收结果写回各阶段总结

验收完成后，回填到 `docs/development/stage-summaries/` 各阶段总结的「验证结果」一节，并把本文档的「后台前端重构」行从「运行时验收整体未做」改为实际结果。**在验收完成前，不得把任何阶段的「验证结果」写成已通过。**

### 第 3 步：权限体系重构

**从现有设计文档出发，不要从零写** —— `开发指导` §19 把《DataOcean 完整权限体系设计》定位为「后续权限重构输入」，它已写明目标：`RBAC + Data Permission + Row Policy + Mask Policy + Permission-aware RAG`。该项工作应**先更新那份文档**，再实施。

三条必须注意：

1. **前后端必须成对改。** 前端当前按 `开发指导` §13 硬编码了「真实 `*` 超级管理员」假设，且 `guards.ts` 中故意不实现 `meta.permission`（2026-09-12 清理死代码时已留注释说明原因）。新模型落地时前端需要专门做一次「消费权限」的改造：把该分支重新加上，并在 7 个业务域、各二级工作区、各 Tab 和写操作上接入权限点。这不是顺手能带的改动，应单独立项。
2. **必须先冻结信息架构。** 新模型要定义菜单权限、页面权限、Tab 权限、操作权限、数据范围，而这五类的划分依据就是业务域 / 工作区 / Tab 的最终形态。
3. **归属权限语义的既有缺口一并处理**：§3.11 的最终权限预览决策端点、§3.13 的权限项。现在单独修会在新模型中返工。（原列入本项的「审批列表未按申请人收窄」已于 2026-09-12 单独修复——后端强制收窄范围不依赖新权限码，见 `开发指导` §12.1。）
4. **验收必须用真实账号。** `开发指导` §13 要求本轮用真实 `*` 账号完成访问，前端不得写死或伪造 `*`；后端现有 `@PreAuthorize`、数据范围、安全规则和状态机全部保留。

### 第 4 步：原有功能待办（按需）

P9 告警触发执行 → P12 告警规则与历史前端页面 → P3 PostgreSQL / Agent workflow 集成测试 → P4 血缘图数据库。

原列入本步的问题中心责任人筛选已于 2026-09-12 完成（提交 `7de957c`），已从 `后续开发.md` 按口径删除。

## 已知工程风险

1. **无 `docker-compose.yml`**，运行时验证不可执行（见文首）。
2. **`V51` 与 `V52` 迁移从未在真实 MySQL 上执行过**：`V51` 的 `UPDATE` 与 `MODIFY COLUMN`、`V52` 的 `ADD COLUMN ... JSON ... AFTER masked_fields` 均未经实机验证。测试环境 `spring.flyway.enabled: false`，自动测试完全不覆盖迁移。首次迁移前建议先备份 `knowledge_doc_version` 表。
3. 前端没有 Vitest 用例；新增或改动关键查询页面时应优先补测试。2026-09-12 的前端改动（含 9 项缺陷修复与新接口接入）**全部只有静态验证**。
4. 前端构建有大包告警；将 ECharts 和血缘可视化按路由拆包。
5. **阶段 0–8 的所有「完成」都只有静态阅读、结构断言与构建验证依据，没有任何一次运行时验收记录**，其中可能仍藏有与 F1–F9 同类的问题。阶段 6–7 是重建、阶段 8 删除了 27 个文件，改动面很大而完全未实测。
6. Python 测试报告 Redis 客户端 `retry_on_timeout` 的弃用警告。

## 相关文档

| 文档 | 用途 |
| --- | --- |
| `docs/review/2026-09-11-后台前端重构审查-前端缺陷清单.md` | F1–F9 的核实、修复与 §7.1 实施记录 |
| `docs/review/2026-09-11-后台前端重构审查-后端事实问题.md` | P0 / P1 / P2 / P3 缺口、C1 / C2 跨端项的证据记录 |
| `docs/development/后续开发.md` | 未完成任务清单（**只记录未完成的事项**：执行轨道 A/B/C、P3 / P4 / P9 / P12、质量扫描未修项、待清理项） |
| `docs/development/stage-summaries/` | 后台前端重构阶段 5–8 的实施总结，以及 §15 强制交付物《原能力迁移对照表》 |
| `docs/development/guides/DataOcean-后台前端整体重构开发指导.md` | 本轮前端重构的开发基线（§16 验收场景、§17 完成定义、§18 禁止事项） |
| `docs/development/guides/DataOcean-后台前端整体重构实施任务清单.md` | 实施批次、文件迁移矩阵、§12 每页完成定义、§15 交付要求 |

## 维护规则

- 每次合并功能后更新本文档的「功能状态、验证状态、主要待办」。
- 只有构建和相应测试通过，模块才能标记为「验证通过」；**静态阅读结论不得写成已验证**。
- `AGENTS.md` 记录协作约束和稳定架构事实；本文档记录会变化的项目状态。
- 2026-08-30 已完成安全收口：移除密码与权限上下文 Redis 缓存；日志值默认脱敏；数据质量 SQL 标识符白名单校验。
