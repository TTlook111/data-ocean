# DataOcean 项目真实状态看板

> 更新日期：2026-08-30
>
> 本文档是当前完成度、验证结果、已知风险和开发优先级的唯一状态入口。`specs/` 目录是早期设计资料，仅用于追溯，不作为当前代码范围或完成度依据。

## 验证基线

| 项目 | 命令 | 当前结果 |
| --- | --- | --- |
| 前端生产构建 | `cd frontend && npm run build` | 通过；仍有大包告警（ECharts 与主入口包超过 500 kB） |
| Java 单元测试 | `cd backend/DataOcean && mvn test` | 116 通过 |
| Python 单元测试 | `cd python-service && uv run pytest -q` | 135 通过、4 跳过、1 个 Redis 客户端弃用警告 |
| Python 依赖锁 | `cd python-service && uv lock --offline --check` | 通过 |

测试运行时禁止向 LangSmith 发送追踪数据，以保证本地 `.env` 中的开发配置不会影响测试结果。

## 当前模块状态

| 模块 | 功能状态 | 验证状态 | 主要待办 |
| --- | --- | --- | --- |
| 用户、认证与权限 | 主链路完成 | Java 单测覆盖部分核心服务 | 补查询权限集成测试 |
| 数据源、元数据与治理 | 主链路完成 | Java 单测覆盖有限 | 数据级质量检查与治理联动的集成测试 |
| skills.md 与 RAG | 发布/向量化链路完成 | Python 回归测试通过 | 真实 Milvus 环境集成验证与性能基线 |
| NL2SQL 与 SQL 沙箱 | 主链路完成 | Python 单测通过 | Agent 全链路、取消和降级集成测试 |
| 审计与数据血缘 | 核心功能完成 | 单测覆盖有限 | 修复 AddLineageDialog 预填实体的小待办 |
| 告警 | 仅规则 CRUD 完成 | 尚无执行验证 | P9：定时评估、通知去重、恢复、历史 |
| 管理端 | 核心页面完成 | 前端构建通过；无前端单测 | P12：告警规则与历史管理界面 |

## 已知工程风险

1. 前端当前没有 Vitest 测试；新增或改动关键查询页面时应优先补测试。
2. 前端构建有大包告警；将 ECharts 和血缘可视化按路由拆包。
3. Python 测试仍报告 Redis 客户端 `retry_on_timeout` 的弃用警告。

## 当前开发顺序

1. P9 告警真实执行：定时评估、告警历史、恢复和去重。
2. P12 管理端闭环：告警规则与历史页面。
3. 测试与性能：查询链路集成测试、前端关键交互测试、前端大包拆分。

## 维护规则

- 每次合并功能后更新本文档的“功能状态、验证状态、主要待办”。
- 只有构建和相应测试通过，模块才能标记为“验证通过”。
- `AGENTS.md` 记录协作约束和稳定架构事实；本文档记录会变化的项目状态。
- 2026-08-30 已完成安全收口：移除密码与权限上下文 Redis 缓存；日志值默认脱敏；数据质量 SQL 标识符白名单校验。
