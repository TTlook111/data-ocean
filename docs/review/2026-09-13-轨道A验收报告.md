# DataOcean 轨道 A：阶段 0–8 真实业务验收报告

> 验收日期：2026-09-13
>
> 验收基线：commit `adc4b8f`（验收过程中未修改产品代码）
>
> 验收依据：`docs/development/项目真实状态看板.md` 第 1 步、开发指导 §16–§18、实施任务清单 §12–§16、任务与实施指导 §14–§16。

## 结论

**阶段 0–8 整体不通过，不能写成“后台前端重构已验收完成”。**

已完成真实环境启动、Java/Milvus/LLM 基础连通、超级管理员登录、数据源创建与采集、部分 API 验证、浏览器页面打开、34 个旧 URL 兼容检查和截图采集；但仍存在以下阻断：

1. Python 测试为 `151 passed, 4 skipped, 1 failed`。
2. Java 知识审核提交真实请求返回 500，知识生命周期写操作无法连续完成。
3. Java 调用 Python 生成知识草稿缺少 `X-Internal-Token`，真实生成请求返回 400。
4. 质量检查触发返回 500；Prompt 启停接口返回 500。
5. 审计列表和操作日志接口对真实 `*` 超级管理员返回 403，相关页面出现“无访问权限”。
6. 问数页直接访问 `?datasourceId=1` 时显示数据源 2，URL 上下文恢复不通过。
7. 没有可用的 Browser/Chrome connector；本轮使用验收专用 Playwright + Chromium 运行时完成浏览器操作，但该运行时位于 `D:\tool`，未写入前端依赖。

## 验收环境与夹具

### 证据位置

- 浏览器完整证据：[轨道A浏览器验收证据.json](D:/projects/DataOcean/output/playwright/轨道A浏览器验收证据.json)
- 浏览器截图目录：[output/playwright](D:/projects/DataOcean/output/playwright)
- 业务库 DDL/种子：[轨道A-业务库.sql](D:/projects/DataOcean/docs/review/fixtures/轨道A-业务库.sql)
- 辅助数据源夹具：[轨道A-辅助数据源.sql](D:/projects/DataOcean/docs/review/fixtures/轨道A-辅助数据源.sql)

### 实际夹具

主业务库 `dataocean_acceptance`：4 张表，数据量如下。

```text
demo_customer    6
demo_product     6
demo_order       12
demo_order_item  21
```

DataOcean 数据源：

```text
1  轨道A验收业务库       dataocean_acceptance
2  轨道A上下文隔离源     dataocean_acceptance_alt
3  轨道A浏览器验收源     dataocean_acceptance_ui
```

`admin` 原始口令无法从 BCrypt 哈希确认。本轮按前置要求生成 BCrypt 哈希并重置，再通过真实页面完成强制改密；明文口令不写入报告。登录后权限查询原始结果包含：

```text
1	admin	5	ADMIN	*,user:manage,role:view,department:manage,datasource:manage,metadata:manage,knowledge:manage,field-tag:manage,feedback:review,security:manage,role:manage,prompt:manage,system:ai-config:view,system:ai-config:manage,prompt:approve
```

## 0.1 浏览器能力自检

### 怎么验

检查 `frontend/package.json`、`output/`、系统命令和当前会话工具列表；随后在验收过程中使用 `D:\tool\dataocean-playwright-runtime` 的 Playwright 和 `D:\tool\dataocean-playwright-browsers` 的 Chromium。

### 实际看到什么

初始仓库检查：

```text
playwright_package_hits=0
output_dir=False
---BROWSER_COMMANDS---
```

后续安装的验收运行时输出：

```text
added 2 packages in 6s
Chrome for Testing 153.0.8010.12 (playwright chromium v1243) downloaded to D:\tool\dataocean-playwright-browsers\chromium-1243
```

浏览器脚本最终输出：

```json
{
  "pages": 62,
  "assertions": 12,
  "legacyRoutes": 34,
  "pageErrors": [],
  "requestFailures": [],
  "evidence": "D:/projects/DataOcean/output/playwright/轨道A浏览器验收证据.json"
}
```

### 结论

**通过（能力已通过外置验收运行时补齐）**。本轮具备真实页面打开、点击、刷新、路由检查和截图能力；不使用 HTML 抓取代替浏览器验收。

## 1.1 后端与迁移

### 1.1.1 `mvn test`

怎么验：

```powershell
cd backend/DataOcean
D:\tool\maven3.9\apache-maven-3.9.16\bin\mvn.cmd test
```

实际看到什么：

```text
[INFO] Tests run: 145, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
[INFO] Total time:  24.057 s
EXIT_CODE=0
```

结论：**通过**。

### 1.1.2 V51 回填结果

怎么验：

```sql
SELECT review_status,COUNT(*)
FROM knowledge_doc_version
GROUP BY review_status
ORDER BY review_status;

SELECT v.doc_id,v.version_no,v.review_status,
       COALESCE(GROUP_CONCAT(DISTINCT t.review_status ORDER BY t.id SEPARATOR ','),'NO_REVIEW_TASK') AS review_tasks
FROM knowledge_doc_version v
LEFT JOIN knowledge_review_task t ON t.doc_version_id=v.id
GROUP BY v.id,v.doc_id,v.version_no,v.review_status
ORDER BY v.doc_id,v.version_no;
```

实际看到什么：

```text
APPROVED  1
PENDING   1
1  1  PENDING   NO_REVIEW_TASK
2  1  APPROVED  REJECTED,APPROVED
```

迁移执行时 `knowledge_doc_version` 为空；以上两条版本行是在迁移之后创建的验收数据。没有能够从迁移前历史行核对的记录。

结论：**无法构造**。V51 文件随 Flyway 成功执行，但本机没有迁移前知识版本，无法证明“历史审核任务可还原，否则为 UNKNOWN”的回填分支。不能把当前 `PENDING` 新行当作 V51 回填证据。

### 1.1.3 V52 字段定义

怎么验：

```sql
SELECT COLUMN_NAME,DATA_TYPE,COLUMN_TYPE,ORDINAL_POSITION
FROM information_schema.COLUMNS
WHERE TABLE_SCHEMA='dataocean'
  AND TABLE_NAME='query_task'
  AND COLUMN_NAME IN ('masked_fields','suggested_questions')
ORDER BY ORDINAL_POSITION;
```

实际看到什么：

```text
masked_fields       varchar  varchar(2000)  19
suggested_questions json     json            20
```

结论：**通过**。`suggested_questions` 为 JSON，且紧接在 `masked_fields` 之后。

### 1.1.4 P0 DRAFT 回滚校验

怎么验：

```http
POST http://127.0.0.1:8080/api/admin/knowledge-docs/1/rollback
Authorization: Bearer <真实 admin JWT>
Content-Type: application/json

{"targetVersionNo":1}
```

实际看到什么：

```text
HTTP_STATUS=400
{
  "code": 400,
  "message": "只有已发布状态的文档才能回滚，当前状态：DRAFT",
  "data": null
}
```

结论：**通过**。DRAFT 文档被后端拒绝回滚。

### 1.1.5 1.1 期间发现的连续流程阻断

怎么验：

```http
POST http://127.0.0.1:8080/api/admin/knowledge-docs/1/submit-review
Authorization: Bearer <真实 admin JWT>
Content-Type: application/json

{}
```

实际看到什么：

```text
{"code":500,"message":"服务器内部错误","data":null}
```

Java 日志原始关键异常：

```text
Caused by: org.apache.ibatis.binding.BindingException:
Parameter 'MP_OPTLOCK_VERSION_ORIGINAL' not found.
Available parameters are [param1, et]
```

结论：**不通过**。真实 DRAFT→PENDING_REVIEW 写操作无法完成；本轮未修改代码。

## 1.2 接口级验证

### 1.2.1 `review-tasks`

怎么验：

```http
GET http://127.0.0.1:8080/api/admin/knowledge-docs/2/review-tasks
Authorization: Bearer <真实 admin JWT>
```

实际看到什么：

```text
HTTP_STATUS=200
{"code":200,"message":"success","data":[{"id":2,"docVersionId":2,"versionNo":1,"reviewStatus":"APPROVED","reviewerId":1,"reviewerName":"超级管理员"},{"id":1,"docVersionId":2,"versionNo":1,"reviewStatus":"REJECTED","reviewerId":1,"reviewerName":"超级管理员"}]}
```

这些记录是为绕过上游生命周期写入失败而直接写入本轮验收夹具的数据。

结论：**通过（读取接口）**；上游真实审核写入不通过。

### 1.2.2 `vector-tasks`

怎么验：

```http
GET http://127.0.0.1:8080/api/admin/knowledge-docs/2/vector-tasks
Authorization: Bearer <真实 admin JWT>
```

实际看到什么：

```text
HTTP_STATUS=200
{"code":200,"message":"success","data":[{"id":1,"datasourceId":1,"targetType":"DOC","targetId":2,"metadataSnapshotId":1,"knowledgeVersionNo":1,"status":"FAILED","errorMessage":"验收夹具：用于验证向量任务查询接口"}]}
```

结论：**通过（读取接口）**。返回了非空向量任务和失败信息；任务本身为验收夹具失败态，不代表向量化成功。

### 1.2.3 `source-snapshots`

怎么验：

```http
GET http://127.0.0.1:8080/api/admin/knowledge-docs/2/source-snapshots
Authorization: Bearer <真实 admin JWT>
```

实际看到什么：

```text
HTTP_STATUS=200
{"code":200,"message":"success","data":[{"versionNo":1,"snapshotId":1,"snapshotVersion":1,"status":"ISSUE_FOUND","tableCount":4,"columnCount":23}]}
```

结论：**通过（读取接口）**。来源快照关联数据非空，来源 ID 来自本轮验收夹具。

### 1.2.4 Prompt 启停

怎么验：

```http
PATCH http://127.0.0.1:8080/api/admin/prompt-templates/sql_generation/enabled
Authorization: Bearer <真实 admin JWT>
Content-Type: application/json

{"enabled":false}
```

实际看到什么：

```text
HTTP_STATUS=500
{"code":500,"message":"服务器内部错误","data":null}
```

重新启用同一接口也返回：

```text
{"code":500,"message":"服务器内部错误","data":null}
```

结论：**不通过**。启停接口在真实运行时失败；原模板读取接口此前返回 `enabled:true`。

### 1.2.5 术语 `APPROVED → DRAFT` 退回

怎么验：

```http
POST http://127.0.0.1:8080/api/admin/glossary/terms/1/revert
Authorization: Bearer <真实 admin JWT>
Content-Type: application/json

{}
```

实际看到什么：

```text
HTTP_STATUS=200
{"code":200,"message":"术语已退回草稿，修改后需重新提交审核","data":null}
```

前置真实响应：术语创建、提交审核、审核通过均返回 `code=200`。

结论：**通过**。

### 1.2.6 质量问题 `assigneeId`

怎么验：

```http
POST http://127.0.0.1:8080/api/admin/snapshots/1/quality-check
Authorization: Bearer <真实 admin JWT>
```

实际看到什么：

```text
HTTP_STATUS=500
{"code":500,"message":"服务器内部错误","data":null}
```

为保证列表接口有可核对数据，使用验收夹具插入一条 `assignee_id=1` 的治理问题后调用：

```http
GET http://127.0.0.1:8080/api/admin/quality-issues?assigneeId=1&page=1&size=20
GET http://127.0.0.1:8080/api/admin/snapshots/1/quality-issues?assigneeId=1&page=1&size=20
Authorization: Bearer <真实 admin JWT>
```

实际看到什么：

```text
HTTP_STATUS=200
{"code":200,"message":"success","data":{"records":[{"id":1,"snapshotId":1,"tableName":"demo_order","columnName":"customer_id","severity":"HIGH","status":"RESOLVED","assigneeId":1,"assigneeName":"超级管理员"}],"total":1,"size":20,"current":1,"pages":1}}

HTTP_STATUS=200
{"code":200,"message":"success","data":{"records":[{"id":1,"snapshotId":1,"tableName":"demo_order","columnName":"customer_id","severity":"HIGH","status":"RESOLVED","assigneeId":1,"assigneeName":"超级管理员"}],"total":1,"size":20,"current":1,"pages":1}}
```

结论：**通过（列表参数和返回过滤结果）**；质量检查触发接口**不通过**，其失败导致正常构造质量问题链路无法完成。

### 1.2.7 访问审批范围收窄

怎么验：使用真实 `admin` 创建一条申请，创建无 `security:manage` 的 `acceptance_user`，再用该用户创建一条申请，并分别调用列表接口。

实际看到什么：

```text
CREATE_NON_SECURITY_USER
{"code":200,"message":"创建成功","data":{"id":2}}

ADMIN_SUBMIT_APPROVAL
{"code":200,"message":"审批请求已提交","data":{"id":1}}

USER_SUBMIT_APPROVAL
{"code":200,"message":"审批请求已提交","data":{"id":2}}

USER_LIST_APPROVALS
{"code":200,"message":"success","data":{"records":[{"id":2,"requesterId":2,"datasourceId":1,"tableName":"demo_order","columnName":"total_amount","status":"PENDING"}],"total":1}}

ADMIN_LIST_APPROVALS
{"code":200,"message":"success","data":{"records":[{"id":1,"requesterId":1,"datasourceId":1,"tableName":"demo_order","columnName":"total_amount","status":"PENDING"},{"id":2,"requesterId":2,"datasourceId":1,"tableName":"demo_order","columnName":"total_amount","status":"PENDING"}],"total":2}}
```

结论：**通过**。非 `security:manage` 用户只看到自己的申请；超级管理员看到全量。

## 1.3 前端点验

### 浏览器运行方式

```powershell
node output/playwright/轨道A浏览器验收.cjs
```

实际结果：

```text
pages=62
assertions=12
legacyRoutes=34
pageErrors=[]
requestFailures=[]
screenshots=70
```

### §16.1 数据源上线

怎么验：浏览器登录 → 数据接入 → 数据源 → 新增数据源；填写 `轨道A浏览器验收源`、本机地址、`dataocean_acceptance_ui`、只读账号；点击“测试连接”→“保存并进入下一步”。

实际看到什么：

```json
{
  "url": "http://127.0.0.1:5173/admin/data-sources/3",
  "testOk": true,
  "body": "连接测试成功 ... 数据源详情 ... 开始采集 ... 数据源已创建"
}
```

截图：

```text
output/playwright/admin-redesign-p2-data-entry-create-connection-tested.png
output/playwright/admin-redesign-p2-data-entry-create-saved.png
output/playwright/admin-redesign-p2-data-entry-ui-created-pass.png
```

后续采集通过 API 形成任务 `SUCCESS` 和快照 v1；正常质量检查返回 500，AI 草稿生成返回 400，知识审核提交返回 500，因此无法从页面连续完成到知识发布和可问数。

结论：**不通过**。数据源创建和连接测试页面通过，但主流程被后端真实失败阻断。

### §16.2 上下文隔离

怎么验：浏览器登录 → 资产目录 `?datasourceId=1&snapshotId=1` → 在顶部数据源选择器切换到“轨道A上下文隔离源”。另从问题中心点击“治理总览”。

实际看到什么：

```json
{
  "visibleSelects": 2,
  "switched": true,
  "url": "http://127.0.0.1:5173/admin/assets?datasourceId=2",
  "before": "轨道A验收业务库 / dataocean_acceptance",
  "after": "轨道A上下文隔离源 / dataocean_acceptance_alt"
}

{
  "clickPath": "治理问题中心 -> 治理总览",
  "observedUrl": "http://127.0.0.1:5173/admin/governance?datasourceId=1&snapshotId=1",
  "conclusion": "通过"
}
```

截图：

```text
output/playwright/admin-redesign-p1-context-switch-datasource-pass.png
output/playwright/admin-redesign-p1-context-workspace-inherit-pass.png
```

结论：**通过（已验证部分）**。顶部切源会清除旧 `snapshotId`，页面对象随数据源变化；工作区导航保留数据源和快照参数。查询入口另有 URL 恢复缺陷，见 §16.1 之后的专项结果。

### §16.3 生命周期

怎么验：打开 `/admin/releases?datasourceId=1`、`/admin/releases/snapshots/1`、`/admin/semantics/knowledge/2?tab=content`，检查 ISSUE_FOUND、INDEXING、知识 Tab 和状态文案；同时调用真实审核/发布写接口。

实际看到什么：

```text
版本 v1
存在问题
ISSUE_FOUND
进入问题中心
批准

知识详情 Tab：内容、来源与覆盖、审核记录、版本、切分预览、索引状态
知识状态：索引中 / INDEXING
```

真实写接口：

```text
POST /api/admin/knowledge-docs/1/submit-review -> 500 服务器内部错误
POST /api/admin/knowledge-docs/2/publish       -> 500 服务器内部错误
```

截图：

```text
output/playwright/admin-redesign-p3-releases-issue-found-pass.png
output/playwright/admin-redesign-p3-snapshot-detail-pass.png
output/playwright/admin-redesign-p5-knowledge-detail-tabs-pass.png
```

结论：**不通过**。状态表达和知识详情 6 Tab 实测存在，但真实生命周期写操作无法连续完成。

### 阶段 5 URL 状态恢复

怎么验：分别打开并刷新：

```text
/admin/semantics/knowledge?tab=review
/admin/semantics/glossaries?glossaryId=1&termId=1
/admin/semantics/prompts?code=sql_generation&tab=versions
```

实际看到什么：

```text
knowledge-review-refresh -> /admin/semantics/knowledge?tab=review -> activeTabs=[审核队列]
glossary-refresh         -> /admin/semantics/glossaries?glossaryId=1&termId=1
prompt-refresh           -> /admin/semantics/prompts?code=sql_generation&tab=versions -> activeTabs=[版本历史]
```

结论：**通过**。

### §16.4 访问与授权体验

怎么验：打开授权管理、访问审批、组织与角色页面；检查超级管理员页面可达性、Tab、数据源范围和页面内数据源选择器。

实际看到什么：

```text
授权管理 Tab：数据源授权、表列策略、最终权限预览
访问审批 Tab：待我审批、已处理、已过期
组织与角色 Tab：用户、角色、部门、权限项
页面文案：数据源由顶部范围条决定，页面内不再另建选择器。
```

授权管理页面在可见 DOM 中有 2 个 combobox，但实际页面内容分别是数据源范围和主体选择，不是两个数据源选择器。

结论：**通过（页面结构和已验证范围）**。完整写操作仍受后端接口失败和审计权限 403 影响。

### §16.5 异常体验

怎么验：Playwright 将 `/api/admin/dashboard/stats` 注入为 HTTP 500，再打开 `/admin/workbench`。

实际看到什么：

```text
页面仍显示数据源准备情况、阻断原因和快捷入口；最近活动区域显示：
Request failed with status code 500
```

控制台错误原始记录：

```text
Failed to load resource: the server responded with a status of 500 (Internal Server Error)
```

结论：**通过（部分失败区域保留）**。页面没有整页变成假空数据；但错误文案未本地化，作为体验问题记录。

### F1–F9

| 缺陷 | 怎么验 | 实际看到什么 | 结论 |
| --- | --- | --- | --- |
| F1 readiness 主操作 | 将数据源 3 通过 API 临时置为 DISABLED，再浏览器打开详情 | 页面显示 `已停用 / DISABLED / 启用数据源` | **通过（禁用态）**；未构造其余全部 readiness 状态 |
| F2 ISSUE_FOUND | 浏览器打开版本发布和快照详情 | `ISSUE_FOUND`、`进入问题中心`、快照详情 `进入治理` | **通过（入口存在）** |
| F3 批量部分成功 | 选中 OPEN + RESOLVED 两条问题，点击批量确认 | `已处理 1 条，1 条因当前状态不允许流转被跳过...` | **通过** |
| F4 工作区高亮 | 打开 `/admin/governance/issues`，读取工作区链接 class | 仅“问题中心”含 `active`，治理总览/规则/字段无误高亮 | **通过** |
| F5 问题分派 | 打开问题详情，选择“验收普通用户”，点击分派 | 抽屉显示“分派责任人”，页面显示 `已分派给 验收普通用户` | **通过** |
| F6 上下文继承 | 问题中心点击“治理总览” | URL 保留 `datasourceId=1&snapshotId=1` | **通过** |
| F7 `stage === 'UNKNOWN'` | 尝试通过现有状态构造 | 当前后端 readiness 没有可用 UNKNOWN 输入 | **无法构造** |
| F8 `known === false` | 使用管理端 readiness actionPath | 当前 5 个 actionPath 均已被映射覆盖 | **无法构造** |
| F9 `SIGN'` 异常分支 | 尝试通过真实接口触发 | 当前没有可触发该后端异常分支的输入 | **无法构造** |

F1 的 8 种 readiness 状态没有全部构造成功，不能把代码映射或单一禁用态扩大为“F1 全部通过”。

### 查询入口 URL 上下文专项

怎么验：浏览器登录后直接打开：

```text
/query?datasourceId=1
```

实际看到什么：

```text
URL: http://127.0.0.1:5173/query?datasourceId=1
页面显示：轨道A上下文隔离源 / 快照待发布
```

结论：**不通过**。URL 指定数据源 1，但页面实际使用数据源 2，违反直接 URL 上下文恢复和数据源隔离要求。

## 1.4 截图交付

怎么验：执行 Playwright 脚本并检查 `output/playwright/`。

实际看到什么：

```text
截图数量：70
证据 JSON：D:/projects/DataOcean/output/playwright/轨道A浏览器验收证据.json
```

截图覆盖登录、数据源创建/详情、资产、快照、治理、F1/F3/F5/F6、知识 6 Tab、授权、运行监控、响应式页面和 34 个旧 URL。截图文件名遵循 `admin-redesign-p{阶段}-{业务域}-{场景}-{状态}.png` 格式；其中旧 URL 的 `pass` 只表示兼容跳转检查完成，页面内具体 API 失败以本报告为准。

结论：**通过（截图已生成）**；阶段整体仍因业务链路缺陷不能通过。

## 阶段 8：34 个旧 URL

每一项均使用 Playwright `page.goto()` 打开，截图和最终地址写入 `轨道A浏览器验收证据.json`。以下是实际最终地址：

| # | 请求 URL | 实际最终 URL | 结论 |
| ---: | --- | --- | --- |
| 1 | `/admin` | `/admin/workbench` | 通过 |
| 2 | `/admin/datasources` | `/admin/data-sources` | 通过 |
| 3 | `/admin/datasources/1/lifecycle` | `/admin/data-sources/1` | 通过，ID 保留 |
| 4 | `/admin/metadata/sync` | `/admin/collections` | 通过 |
| 5 | `/admin/metadata/schedule` | `/admin/collections?tab=schedule` | 通过，Tab 保留 |
| 6 | `/admin/metadata/catalog` | `/admin/assets` | 通过 |
| 7 | `/admin/metadata/tables` | `/admin/assets?view=tables` | 通过，参数保留 |
| 8 | `/admin/metadata/lifecycle` | `/admin/releases` | 通过 |
| 9 | `/admin/metadata/snapshots` | `/admin/releases` | 通过 |
| 10 | `/admin/metadata/version-history` | `/admin/releases?tab=history` | 通过，Tab 保留 |
| 11 | `/admin/metadata/diff?oldId=1&newId=1` | `/admin/releases/snapshots/1/diff/1` | 通过，ID 保留 |
| 12 | `/admin/governance/quality` | `/admin/governance` | 通过 |
| 13 | `/admin/governance/issues` | `/admin/governance/issues` | 通过，原路由保留 |
| 14 | `/admin/governance/status` | `/admin/governance/rules?tab=status` | 通过，Tab 保留 |
| 15 | `/admin/field/tags` | `/admin/governance/fields?tab=tags` | 通过，Tab 保留 |
| 16 | `/admin/field/confidence` | `/admin/governance/fields?tab=confidence` | 通过，Tab 保留 |
| 17 | `/admin/field/feedback-review` | `/admin/governance/fields?tab=feedback` | 通过，Tab 转换 |
| 18 | `/admin/glossary/list` | `/admin/semantics/glossaries?glossaryId=1` | 通过，ID 保留 |
| 19 | `/admin/knowledge` | `/admin/semantics/knowledge` | 通过 |
| 20 | `/admin/knowledge/editor/2` | `/admin/semantics/knowledge/2?tab=content` | 通过，ID/Tab 保留 |
| 21 | `/admin/knowledge/versions/2` | `/admin/semantics/knowledge/2?tab=versions` | 通过，ID/Tab 保留 |
| 22 | `/admin/knowledge/review` | `/admin/semantics/knowledge?tab=review` | 通过，Tab 保留 |
| 23 | `/admin/prompts` | `/admin/semantics/prompts?code=chart_generation` | 通过，进入 Prompt 详情 |
| 24 | `/admin/permission/access` | `/admin/access?tab=grants` | 通过，Tab 保留 |
| 25 | `/admin/permission/policies` | `/admin/access?tab=policies` | 通过，Tab 保留 |
| 26 | `/admin/users` | `/admin/access/organization?tab=users` | 通过，Tab 保留 |
| 27 | `/admin/roles` | `/admin/access/organization?tab=roles` | 通过，Tab 保留 |
| 28 | `/admin/departments` | `/admin/access/organization?tab=departments` | 通过，Tab 保留 |
| 29 | `/admin/audit/logs` | `/admin/operations/queries?tab=audit` | 通过，页面数据 403 |
| 30 | `/admin/audit/slow-queries` | `/admin/operations/queries?tab=performance` | 通过，页面数据 403 |
| 31 | `/admin/audit/data-lineage` | `/admin/operations/lineage` | 通过 |
| 32 | `/admin/system/health` | `/admin/platform/runtime` | 通过 |
| 33 | `/admin/system/operation-logs` | `/admin/platform/operation-logs` | 通过，页面数据 403 |
| 34 | `/admin/system/ai-config` | `/admin/platform/ai` | 通过 |

结论：**旧 URL 路由兼容 34/34 通过**；这不等于目标页面全部业务功能通过，29、30、33 的数据接口仍有 403。

## 验收边界

- 本轮没有创建、删除、重建或停止任何 Docker 容器和数据卷。
- 没有执行 `down`、`rm`、`-v`、`flyway clean`，没有重建 `dataocean` 库。
- 接受前置要求，新增了独立业务库、辅助业务库和 DataOcean 验收记录；这些不是产品功能代码。
- 没有向 LangSmith 发送追踪数据；Python `.env` 的 `LANGCHAIN_TRACING_V2=false` 保持关闭。
- 没有修改 Java、Python、前端产品代码，也没有修改测试断言。
- 1.3 浏览器操作使用外置 Playwright/Chromium 运行时完成；没有 Browser/Chrome connector，因此没有把平台连接器能力写成已存在。
- F7 `stage=UNKNOWN`、F8 `known=false`、F9 `SIGN'` 按要求记为“无法构造”，没有把不可构造当作缺陷。
- 当前结论是部分验收证据，不是阶段 0–8 完成证明；修复阻断问题后必须重新执行本轨道并产生新的报告。
