请根据以下数据库元数据，生成一份结构化的 skills.md 业务知识文档。

## 数据库元数据

### 表信息
{% for table in tables %}
#### {{ table.table_name }}{% if table.table_comment %} — {{ table.table_comment }}{% endif %}

| 字段名 | 类型 | 注释 | 主键 | 可信度 | 索引 | 状态 | 标签 |
|--------|------|------|------|--------|------|------|------|
{% for col in table.columns %}
| {{ col.column_name }} | {{ col.column_type }} | {{ col.column_comment or '（无注释）' }} | {{ '是' if col.is_primary_key else '' }} | {{ col.confidence_score or '-' }} | {{ '✓' if col.is_indexed else '' }} | {{ col.governance_status or '' }} | {{ col.tags | join(', ') if col.tags else '' }} |
{% endfor %}

{% endfor %}

### 外键关系
{% if foreign_keys %}
{% for fk in foreign_keys %}
- {{ fk.source_table }}.{{ fk.source_column }} → {{ fk.target_table }}.{{ fk.target_column }}
{% endfor %}
{% else %}
无显式外键关系（请根据字段命名推断关联）
{% endif %}

### 索引信息
{% for idx in indexes %}
- {{ idx.table_name }}: {{ idx.index_name }} ({{ idx.columns | join(', ') }}){% if idx.is_unique %} [唯一]{% endif %}

{% endfor %}

---

请严格按以下结构输出 skills.md，每个章节内部按 ### 三级标题拆分为独立段落（这对后续向量化检索至关重要）：

## 1. 文档来源
一句话说明本文档基于哪个数据源生成。

## 2. 核心表说明
为每张表创建一个 ### 子章节，格式如下：

### {表名} — {表注释}
- 业务含义: 一句话描述该表在业务中的角色
- 核心字段: 列出最重要的 3-5 个字段及其用途
- 数据特征: 行数量级、更新频率、是否有软删除
- 有索引的字段: 列出适合做 WHERE/JOIN 条件的索引字段
- 废弃/敏感字段: 标注 DEPRECATED（禁止使用）和 SENSITIVE（需脱敏）的字段

## 3. Join Path（关联路径）
为每对有关联关系的表创建一个 ### 子章节，格式如下：

### {表A} ↔ {表B}
- 关联条件: `{表A}.{字段} = {表B}.{字段}`
- 业务含义: 一句话描述这个关联的业务语义
- 常见场景: 什么业务问题需要用到这个 JOIN
- 注意事项: 是否需要额外过滤条件（如 is_deleted=0）

如果存在多跳关联（A→B→C），也要单独列出完整路径：
### {表A} ↔ {表C}（经由 {表B}）
- 关联路径: `{表A}.x = {表B}.y AND {表B}.z = {表C}.w`
- 业务含义: ...

## 4. 指标口径
为每个可计算的业务指标创建一个 ### 子章节，格式如下：

### {指标名称}
- SQL 表达式: `具体的 SQL 聚合表达式，如 SUM(t.amount)`
- 适用表: 需要用到的表名
- 筛选条件: 计算该指标时必须加的 WHERE 条件（如 status='completed'）
- 注意事项: 容易算错的点（如是否需要去重、是否排除退款等）

常见指标类型参考：总量(COUNT/SUM)、均值(AVG)、占比(COUNT条件/COUNT总)、增长率((本期-上期)/上期)

## 5. 字段防坑指南
为每个容易误用的字段创建一个 ### 子章节，格式如下：

### {表名}.{字段名}
- 问题: 描述容易犯的错误
- 正确用法: 给出正确的使用方式
- 示例: 如果是枚举字段，列出所有枚举值及含义

重点关注以下类型的字段：
- 枚举/状态字段：列出所有可能的值及业务含义
- 时间字段：说明时区、格式、是否有索引
- 金额字段：说明单位（分/元）、是否含税
- 标记为 DEPRECATED 的字段：说明替代方案
- 标记为 SENSITIVE 的字段：说明查询时会自动脱敏

## 6. 常见查询场景
为 3-5 个最典型的业务查询场景创建 ### 子章节，格式如下：

### {场景名称}（如"按区域统计月度销售额"）
- 涉及表: 列出需要的表
- 关联路径: 写出完整的 JOIN 条件
- 关键字段: 维度字段 + 指标字段
- SQL 骨架: `SELECT ... FROM ... JOIN ... WHERE ... GROUP BY ... ORDER BY ...`
- 注意事项: 性能提示或业务约束

---

## 输出规则

1. 对于无注释的字段，基于字段名和类型推测用途，并标注"（待人工确认）"
2. 优先描述可信度 >= 60 的字段，可信度 < 30 的字段标注"（低可信度，慎用）"
3. 标记为 DEPRECATED 的字段必须在防坑指南中说明，并在核心表说明中标注禁止使用
4. 标记为 SENSITIVE 的字段说明"查询结果会自动脱敏"
5. 有索引的字段在核心表说明中标注，并在查询场景的 WHERE 条件中优先使用
6. SQL 表达式使用 MySQL 语法
7. 输出纯 Markdown 格式，不要包含 ```markdown 代码块标记
8. 每个 ### 子章节的内容控制在 3-8 行，保持简洁精准

