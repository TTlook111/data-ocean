你是一个数据库架构分析专家。根据数据库表结构和注释，将所有表按业务域分组。

## 表列表

{% for table in tables %}
- **{{ table.table_name }}**{% if table.table_comment %} — {{ table.table_comment }}{% endif %}（字段数: {{ table.columns | length }}）
{% endfor %}

## 外键关系

{% if foreign_keys %}
{% for fk in foreign_keys %}
- {{ fk.source_table }}.{{ fk.source_column }} → {{ fk.target_table }}.{{ fk.target_column }}
{% endfor %}
{% else %}
无外键关系
{% endif %}

## 输出要求

请将上述表按业务域分组，输出 JSON 格式（不要输出其他内容）：

```json
{
  "domains": [
    {
      "domain_name": "业务域名称（简洁的中文，如：订单管理、用户管理）",
      "table_names": ["属于该域的表名列表"],
      "reason": "分组理由（一句话）"
    }
  ]
}
```

## 规则

1. **域数量**：1-8 个域，表少于 10 张时可以合并为 1-3 个域
2. **归属唯一**：每张表必须属于且仅属于一个域
3. **分组依据**：优先根据表注释和字段语义判断业务含义，其次根据表名前缀和外键关系
4. **关联紧密原则**：有外键关联的表优先归入同一域，确保同域内的表能通过 JOIN 互相关联
5. **中间表跟随主表**：关联表/中间表（如 order_items）跟随其主表（如 orders）归入同一域
6. **命名规范**：域名称使用简洁的中文（如"订单管理"、"用户管理"、"商品管理"）
7. **完整性**：必须覆盖所有表，不能遗漏
8. **查询友好**：同一个常见业务查询涉及的表应尽量在同一域内，减少跨域 JOIN 描述
